{
  "name": "@glass/cloud-infrastructure",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('Cloud infrastructure is reserved for Milestone 3; no deployment is configured.')\"",
    "test": "vp test run --passWithNoTests",
    "typecheck": "tsgo --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vite-plus": "catalog:"
  }
}
